# Server
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# PostgreSQL
DATABASE_URL=postgresql://postgres:SqzcMFhIuKwmEQxfzmSvWvpgLEyODaqI@hayabusa.proxy.rlwy.net:34165/railway
# or use individual params
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=kalro_farm

# JWT
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Admin bootstrap (used by seed)
ADMIN_EMAIL=admin@kalrofarm.co.ke
ADMIN_PASSWORD=Admin@1234
ADMIN_NAME=Kalro Admin

# Business
WHATSAPP_NUMBER=254756908482
PHONE_NUMBER=0756908482
BUSINESS_EMAIL=info@kalrofarm.co.ke
BUSINESS_LOCATION=Naivasha, Kenya

# Uploads — choose ONE persistence strategy (required on Railway)
#
# OPTION A (recommended): Cloud object storage — survives every deploy
# STORAGE_TYPE=s3
# S3_BUCKET=kalro-uploads
# S3_REGION=auto
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_PUBLIC_BASE_URL=https://pub-xxxx.r2.dev
# S3_FORCE_PATH_STYLE=true
#
# OPTION B: Railway Volume — mount at /app/server/uploads on your web service
# STORAGE_TYPE=local
# UPLOAD_DIR=/app/server/uploads
#
# Without Option A or B, uploaded images are LOST on every redeploy.
# STORAGE_TYPE=local
# UPLOAD_DIR=/app/server/uploads
